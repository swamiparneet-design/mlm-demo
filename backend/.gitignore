# Build results
[Bb]in/
[Oo]bj/

# Visual Studio / Rider / VS Code
.vs/
.vscode/
.idea/
*.user
*.userosscache
*.suo
*.userprefs

# EF Core migrations bundle output (if generated locally)
*.dll
efbundle*

# Rider
*.sln.iml

# NuGet
*.nupkg
**/[Pp]ackages/*
!**/[Pp]ackages/build/

# Test results
[Tt]est[Rr]esult*/
*.trx
*.coverage
*.coveragexml

# ASP.NET Core secrets (local machine only, never commit real secrets).
# appsettings.Development.json is currently tracked because it has no secrets
# in it today - if you ever add a real connection string / API key / JWT
# secret to it, uncomment the line below (or move the secret to user-secrets /
# environment variables instead).
# appsettings.Development.json
appsettings.*.local.json
secrets.json

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
